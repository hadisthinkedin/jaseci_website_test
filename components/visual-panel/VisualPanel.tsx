/**
 * VisualPanel — placeholder section that sits right after the Hero
 * (which contains the benchmark widget). Content (lottie / images,
 * copy) will be swapped in once provided.
 */
export default function VisualPanel() {
  return (
    <div className="section-base_visual">
      <div className="img-cover" aria-hidden="true" />
    </div>
  );
}
